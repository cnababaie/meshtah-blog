version: "3.9"

services:

  strapi:
    build: ./meshtah-strapi-back
    command: sh -c "npm install && npm run build && npm run start"
    ports:
      - "1337:1337"
    volumes:
      - ./meshtah-strapi-back:/app
      - /app/node_modules
    environment:
      NODE_ENV: production
      DATABASE_CLIENT: sqlite
      DATABASE_FILENAME: /app/.tmp/data.db
      ADMIN_JWT_SECRET: supersecretadminjwt
      API_TOKEN_SALT: supersecrettoken
      STRAPI_ADMIN_BACKEND_URL: http://meshtah.net/api
    restart: unless-stopped

  next:
    build: ./meshtah-next-front
    command: sh -c "npm install && npm run build && npm run start"
    ports:
      - "3000:3000"
    volumes:
      - ./meshtah-next-front:/app
      - /app/node_modules
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API: http://127.0.0.1:1337/api
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./nginx/ssl:/etc/letsencrypt:ro
    depends_on:
      - strapi
      - next
    restart: unless-stopped

