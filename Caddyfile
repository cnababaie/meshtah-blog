meshtah.net {
    encode gzip

    # API → Strapi (keeps /api prefix)
    handle /api/* {
        reverse_proxy strapi:1337
    }

    # Everything else → Next.js
    handle {
        reverse_proxy next:3000
    }
}

